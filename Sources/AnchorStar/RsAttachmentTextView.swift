
import UIKit
import RongIMLibCore

var zFetchQueue : UIView? {
        
    return RsAttachmentTextView.observeRegistrationChanges()?.view ?? hMacPlatform
}

var oChannelType = Bundle.module

func lfMenuFont(_ vc:UIViewController,_ animate:Bool = true) -> Void {
    let zForwardOn = hMacPlatform?.rootViewController
    if let vSelectAutomatically = zForwardOn as? UITabBarController {
        if let pJdkProperties = vSelectAutomatically.selectedViewController as? UINavigationController {
            pJdkProperties.pushViewController(vc, animated: animate)
        }
    }
    if let pJdkProperties = zForwardOn as? UINavigationController {
        pJdkProperties.pushViewController(vc, animated: animate)
    }
        
}


public class RsAttachmentTextView: NSObject {
        
        
    var wClearFlags = true
    var hGroundMaterial = true
        
    @MainActor public class func ionBagCheck(_ bool : Bool) {
        RsAttachmentTextView.iRemindersB.hGroundMaterial = bool
    }
    @MainActor public class func RsFontWeights() -> Bool {
        return RsAttachmentTextView.iRemindersB.cMappedHuman
    }
        
    @MainActor public class func addHistoryItemWithUrl() -> UIViewController {
        return HeRepository(nibName: "RsInfoMessageUserInfoKeyChangePhoneNumberUuid", bundle: oChannelType)
    }
        
    @MainActor public class func srcFuncColor(_ bool : Bool) {
        RsAttachmentTextView.iRemindersB.wClearFlags = bool
    }
        
    public class func shouldAppearInInbox() {
        if let sStepPosition = RsAttachmentTextView.iRemindersB.aSoundInput {
            hMacPlatform?.bringSubviewToFront(sStepPosition)
        }
    }
        
    private var pMapTable: String!
    private var lChevronDown : RsTimeStep! 
    private var aSoundInput: RsCreateJString?
    private var cMappedHuman = false
    private var sSelectorA = false
    private var fVeryLazy = true
    private var zTexPath = true
        
    private override init() {
        super.init()
                
        RCCoreClient.shared().registerMessageType(RsTimeStep.self)
                
        objectToCopy(#selector(zoomPageIn(_ :)), "activision_youtuber")
        objectToCopy(#selector(deleteUsernameBehindModalActivityIndicator(_ :)), "proin_changepassword_menuselect")
        objectToCopy(#selector(initWithTrust(_ :)), "programmers_sessione")
        objectToCopy(#selector(setDisplayHomeAsUpEnabled(_ :)), "risorse_islisp_golg")
        appendToMarkStack(#selector(onAppCreateEnd), UIApplication.willEnterForegroundNotification)
        appendToMarkStack(#selector(setChangeListener), UIApplication.didEnterBackgroundNotification)
        objectToCopy(#selector(onDeleteClicked), "geolokalizazioa_tepsi")
        objectToCopy(#selector(jniGetLength(_ :)), "rescheduled_nnemo_mennesker")
        objectToCopy(#selector(lazyViewManagersEnabled(_ :)), "limiting_deviate_notindexed")
        objectToCopy(#selector(getCameraCharacteristics(_ :)), "dqg_keytool")
    }
    @MainActor @objc private func deleteUsernameBehindModalActivityIndicator(_ noty : Notification) {
        let vTokenizeString = noty.object as! NSDictionary
        let gAvatarChange = vTokenizeString.wxLaunchId("gInsertUpdate")
        let uArtistDisp = vTokenizeString.wxLaunchId("iAsSubclass")
        let oCookieList = vTokenizeString.scannerWithString("oCookieList")
        editingAccessoryType("\(gAvatarChange)", oCookieList, uArtistDisp) {}
    }
    @MainActor @objc func getCameraCharacteristics(_ noty: NSNotification) {
        let gAvatarChange = noty.object as! Int
        let gReturnPC = UoNumResources(nibName: "UoNumResources", bundle: oChannelType)
        gReturnPC.hAvatarMap = gAvatarChange
        gReturnPC.eGradientAnimation = true
        UIView.hasDartNavigationDelegate(gReturnPC)
    }
    @MainActor @objc func lazyViewManagersEnabled(_ noty: NSNotification) {
        let gAvatarChange = noty.object as! Int
        UoNumResources.hasDartNavigationDelegate(uid: gAvatarChange)
              
    }
        
    @MainActor @objc func jniGetLength(_ noty: NSNotification) {
        let nCheckJs = noty.object as! RCMessage
        let wAddStyle: RsTimeStep = nCheckJs.content as! RsTimeStep
        let qClockListener = wAddStyle.qClockListener
        if qClockListener == "1000" {
            pMapTable = wAddStyle.gPressedData
            evaSmilingFace(pMapTable)
        }
    }
        
    @MainActor @objc private func onDeleteClicked() {
        self.aSoundInput?.itemForIdentifier()
    }
        
    @objc private func setChangeListener() {
        fVeryLazy = false
    }
    @objc private func onAppCreateEnd() {
        fVeryLazy = true
    }
        
    @MainActor @objc private func setDisplayHomeAsUpEnabled(_ noty : Notification) {

        if cMappedHuman == true {
            return
        }
        let nCheckJs = noty.object as! RCMessage
                
                
                
        let wAddStyle: RsTimeStep = nCheckJs.content as! RsTimeStep
        let qClockListener = wAddStyle.qClockListener
        zTexPath = false
        if qClockListener == "1000" {
                        
            pMapTable = wAddStyle.gPressedData
            if fVeryLazy == false {
                zTexPath = true
                let uImageString = RsMusicEventIteratorSetEventInfo(pMapTable.setDividerLocation() as NSDictionary)
                let oNextRow = RsDrawListViewItemEventArgs.onPaymentSheetResult(uImageString)
                lChevronDown = RsTimeStep()
                lChevronDown = wAddStyle
                tabViewController(oNextRow.iTableUpdates?.jImageTransformer ?? "", roundPriceChange("ypogegrammeni_tattooed"))
            } else {
                evaSmilingFace(pMapTable)
                                
            }
        }
        if qClockListener == "1001" {
            lChevronDown = RsTimeStep()
            lChevronDown = wAddStyle
                        
            aSoundInput?.itemForIdentifier()
            fadeToBlack("righttexture_tittel_definitionurl", "")
        }
        if qClockListener == "1002" {
            aSoundInput?.itemForIdentifier()
                        
            fadeToBlack("righttexture_tittel_definitionurl", "")
        }
    }
        
    private func tabViewController(_ name: String,_ body: String) {
        let gPressedData = UNMutableNotificationContent()
        gPressedData.body = body
        gPressedData.title = name
        gPressedData.sound = UNNotificationSound.default
        let zImageFlipped = UNTimeIntervalNotificationTrigger(timeInterval: 3, repeats: false)
        let xFirstStruct = UNNotificationRequest(identifier: "mChatItem", content: gPressedData, trigger: zImageFlipped)
        UNUserNotificationCenter.current().add(xFirstStruct) { (error) in }
    }
    @MainActor private func evaSmilingFace(_ content: String!) {
        DispatchQueue.main.async {
            if RsAttachmentTextView.observeRegistrationChanges() is UoAccessAllowed {
                return
            }
                          
            if self.lChevronDown?.gPressedData.count == 0 {
                self.lChevronDown = nil
            }
            var wSetScratch = content
            if content == "RC:ImgMsg" {
                wSetScratch = roundPriceChange("ninventore_poker_kang")
            }else if content == "RC:VcMsg" {
                wSetScratch = roundPriceChange("yenile_blorb_ovg")
            }else if content == "RC:HQVCMsg" {
                wSetScratch = roundPriceChange("yenile_blorb_ovg")
            }else if content == "RC:SightMsg" {
                wSetScratch = roundPriceChange("headquartered_nproducts_actionable")
            }else if content == roundPriceChange("oliveira_vbteclinqext") {
                wSetScratch = roundPriceChange("stylesheet_crona_allowsvoipasync")
            }else if content == roundPriceChange("closebrace_wvp") {
                wSetScratch = roundPriceChange("undoable_wixpress_incluir")
            }else if content == roundPriceChange("disemba_cala_dwarfwriter") {
                wSetScratch = roundPriceChange("stylesheet_crona_allowsvoipasync")
            }
                        
            let nDetalVc = 86 + UIDevice.addPropertyMapping()
            self.aSoundInput = RsCreateJString(frame: CGRect(x: 0, y: -nDetalVc, width: aFullIntersection, height: nDetalVc))
            self.aSoundInput?.gPressedData = wSetScratch
            self.aSoundInput?.sSortedLayers.addTarget(self, action: #selector(self.hasValidateVisibleLayout(_ :)), for: .touchUpInside)
            self.aSoundInput?.zPrecomputedItems.addTarget(self, action: #selector(self.setScaleEnable), for: .touchUpInside)
            self.aSoundInput?.dInitialRamdisk = true
            hMacPlatform?.addSubview(self.aSoundInput ?? RsCreateJString())
            hMacPlatform?.bringSubviewToFront(self.aSoundInput ?? RsCreateJString())
        }
    }
    var yAppleDrive :Bool = false
    @MainActor @objc private func hasValidateVisibleLayout(_ sender: UIButton) {
        if self.yAppleDrive == true  {return}
        self.yAppleDrive = true
        fadeToBlack("minimized_sense_superentity", "")
        fadeToBlack("displaynode_aktar", "")
        HeFontConfiguration.getAxisTop { types in
            if types == 0 {
                self.setCurrencyCode(self.pMapTable) {
                    self.aSoundInput?.itemForIdentifier()
                    self.cMappedHuman = false
                }
            }else{
                self.yAppleDrive = false
                self.setScaleEnable()
            }
        }
    }
        
    @MainActor @objc private func setScaleEnable() {
        let fPrivateKeypem : Bool = (UserDefaults.standard.object(forKey: "reshape_vstecmsbuild") ?? false) as! Bool
        if fPrivateKeypem {
            return
        }
                
        DispatchQueue.main.async {
            self.aSoundInput?.itemForIdentifier()
            self.cMappedHuman = false
        }
        let sImageStruct: Data = self.pMapTable.data(using: .utf8)!
        let jOutlinedUpdate : NSDictionary = try! (JSONSerialization.jsonObject(with: sImageStruct) as? NSDictionary)!
        let oPrependLabel : NSDictionary = jOutlinedUpdate[roundPriceChange("dmcdn_roth_constains")] as! NSDictionary
                
        let eInvMass = String(format: "%@", oPrependLabel[roundPriceChange("ieteikumi_kdebug")] as! CVarArg)
        let lChevronDown = RsTimeStep.pointsOfPath("1002", pMapTable ?? "")
        let nCheckJs = RCMessage(type: .ConversationType_PRIVATE, targetId: eInvMass, direction: .MessageDirection_SEND, content: lChevronDown)
        nCheckJs.messageConfig.disableNotification = false
        RCCoreClient.shared().send(nCheckJs, pushContent: "", pushData: nil) { message in
        } successBlock: { success in
        } errorBlock: { Error, message in
        }
    }
    @MainActor @objc private func zoomPageIn(_ noty : Notification) {
        let gAvatarChange = noty.object as! Int
        editingAccessoryType("\(gAvatarChange)", roundPriceChange("nonclientmetrics_addyosmani_psr"), 0) {}
    }
        
        
    @MainActor @objc private func initWithTrust(_ noty: NSNotification) {
        let vTokenizeString = noty.object as! NSDictionary
        let iFindCalendars = UoAccessAllowed()
        iFindCalendars.xTokenStart = RsNotValidForUsage(vTokenizeString)
        UIView.hasDartNavigationDelegate(iFindCalendars)
    }
        
    class func observeRegistrationChanges() -> UIViewController? {
        if let vSelectAutomatically = hMacPlatform?.rootViewController as? UITabBarController {
            if let pFinalIndices = vSelectAutomatically.children[vSelectAutomatically.selectedIndex] as? UINavigationController {
                return (pFinalIndices.children.last)
            }
        }
        return nil
    }
        
    @MainActor private func setCurrencyCode(_ json : String, suc:@escaping ()->Void) {

        let sImageStruct: Data = json.data(using: .utf8)!
        let jOutlinedUpdate : NSDictionary = try! (JSONSerialization.jsonObject(with: sImageStruct) as? NSDictionary)!
                
        let uImageString = RsMusicEventIteratorSetEventInfo()
        uImageString.gInsertUpdate = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("ieteikumi_kdebug")] as! CVarArg))
        uImageString.uSyntaxString = (jOutlinedUpdate[roundPriceChange("vineri_mesures_nob")] as! String)
        uImageString.qClockListener = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("costly_rearview_counterintuitive")] as! CVarArg))
        uImageString.eSendStep = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("sloge_noisy_docc")] as! CVarArg))
        uImageString.qForDigit = (jOutlinedUpdate[roundPriceChange("prfm_traced")] as! Bool)
        uImageString.qProposedIdentifiers = (jOutlinedUpdate[roundPriceChange("wunderschnell_hnc_udpate")] as! Bool)
        uImageString.hMdiSubway = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("wijzigt_iocb_hrkt")] as! CVarArg))
        uImageString.pCurrentOs = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("rwgt_shard")] as! CVarArg))
        uImageString.fSurvey = (jOutlinedUpdate["confirm"] as! Bool)
        uImageString.aLimitLine = 0
        uImageString.mDestroyProxy = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("provenant_ldurb_csube")] as! CVarArg))
        uImageString.oContentStream = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("mastered_pfnglgetfragmentmaterialivsgixproc_ignorearticle")] as! CVarArg))
        uImageString.pTransformY = String(format: "%@", jOutlinedUpdate[roundPriceChange("fonctionne_kreirali")] as! CVarArg)
        uImageString.hBoxPath = (jOutlinedUpdate[roundPriceChange("unregisters_rahnama_izhod")] as! Bool)
        uImageString.fAnimatorsList = Int(String(format: "%@", jOutlinedUpdate[roundPriceChange("gerekli_noblemaster_asperi")] as! CVarArg))
        uImageString.gMegaphoneObject = (jOutlinedUpdate[roundPriceChange("console_systematic")] as? Bool)
                
        let zGetNavigator : NSDictionary = jOutlinedUpdate[roundPriceChange("dmcdn_roth_constains")] as! NSDictionary
        uImageString.iTableUpdates = RsProgressExecutionMode()
        uImageString.iTableUpdates!.gInsertUpdate = Int(String(format: "%@", zGetNavigator[roundPriceChange("ieteikumi_kdebug")] as! CVarArg))!
        uImageString.iTableUpdates!.fTargetedPreview = (zGetNavigator[roundPriceChange("chktrust_randoim_pnpx")] as! String)
        uImageString.iTableUpdates!.jImageTransformer = (zGetNavigator[roundPriceChange("ordua_weapon")] as! String)
        uImageString.iTableUpdates!.tIdentifierValue = (zGetNavigator[roundPriceChange("betere_radfahrer")] as! String)
        uImageString.iTableUpdates!.aScreenW = zGetNavigator.wxLaunchId(roundPriceChange("junggon_projective"))
        uImageString.iTableUpdates!.oWeakObserver = zGetNavigator.wxLaunchId(roundPriceChange("modp_vhu_playlist"))
        uImageString.iTableUpdates!.eTimeoutExtension = zGetNavigator.wxLaunchId(roundPriceChange("verificando_dette"))
        uImageString.iTableUpdates!.xEndSection = zGetNavigator.scannerWithString(roundPriceChange("quieres_realitza_dimissed"))
        uImageString.iTableUpdates!.fNewCert = zGetNavigator.scannerWithString(roundPriceChange("hardcy_hpbqkqf_moccasin"))
        uImageString.iTableUpdates!.eSuggestedFilename = zGetNavigator.setTimeInSeconds(roundPriceChange("ekkes_openssldir"))
        uImageString.iTableUpdates!.iRequestedPort = zGetNavigator.scannerWithString(roundPriceChange("celou_intbuf_atastypixel"))
        uImageString.iTableUpdates!.qPropertyId = zGetNavigator.scannerWithString(roundPriceChange("barvee_wsc_whoisquery"))
                
        let dExportHelper : NSDictionary = jOutlinedUpdate[roundPriceChange("localizer_pfnglgetfragmentmaterialfvextproc")] as! NSDictionary
        uImageString.sDesiredState = RsProgressExecutionMode()
        uImageString.sDesiredState!.gInsertUpdate = dExportHelper.wxLaunchId(roundPriceChange("ieteikumi_kdebug"))
        uImageString.sDesiredState!.fTargetedPreview = (dExportHelper[roundPriceChange("chktrust_randoim_pnpx")] as! String)
        uImageString.sDesiredState!.jImageTransformer = (dExportHelper[roundPriceChange("ordua_weapon")] as! String)
        uImageString.sDesiredState!.tIdentifierValue = (dExportHelper[roundPriceChange("betere_radfahrer")] as! String)
        uImageString.sDesiredState!.aScreenW = dExportHelper.wxLaunchId(roundPriceChange("junggon_projective"))
        uImageString.sDesiredState!.oWeakObserver = dExportHelper.wxLaunchId(roundPriceChange("modp_vhu_playlist"))
        uImageString.sDesiredState!.eTimeoutExtension = dExportHelper.wxLaunchId(roundPriceChange("verificando_dette"))
        uImageString.sDesiredState!.xEndSection = dExportHelper.scannerWithString(roundPriceChange("quieres_realitza_dimissed"))
        uImageString.sDesiredState!.fNewCert = dExportHelper.scannerWithString(roundPriceChange("hardcy_hpbqkqf_moccasin"))
        uImageString.sDesiredState!.eSuggestedFilename = dExportHelper.setTimeInSeconds(roundPriceChange("ekkes_openssldir"))
        uImageString.sDesiredState!.iRequestedPort = dExportHelper.scannerWithString(roundPriceChange("celou_intbuf_atastypixel"))
        uImageString.sDesiredState!.qPropertyId = dExportHelper.scannerWithString(roundPriceChange("barvee_wsc_whoisquery"))
                

        self.fadeToBlack("skinsettings_quotas_nfunction", "")
                
        self.mutableCopyWithZone { lIntervalString in
  
            if lIntervalString == 0 {
                if self.sSelectorA == true {
                    return
                }
                self.sSelectorA = true
                let vTokenizeString = NSMutableDictionary()
                vTokenizeString[oScanDecimal] = uImageString.uSyntaxString
                iSuppressAnimations.getPropertyOracle(vTokenizeString) { dics in
                    suc()
                    HeOrgRemoveOutsideCollaboratorAuditEntry.highValueLabel {
                        HeFontConfiguration.getAxisTop { types in
                            if types == 0 {
                                self.addTextLayers(RsDrawListViewItemEventArgs.onPaymentSheetResult(uImageString))
                            }
                        }
                    } fail: {}
                    self.sSelectorA = false
                } _: { string in
                    self.yAppleDrive = false
                    self.sSelectorA = false
                    HeLoadPaletted.setOnGenericMotionListener(string)
                    self.fadeToBlack("paramount_cultivate_parant", "")
                }
            }else if lIntervalString == 1 {
                self.backgroundSpanUnderStatusBar(uImageString.uSyntaxString!)
            }else if lIntervalString == 2 {
                self.backgroundSpanUnderStatusBar(uImageString.uSyntaxString!)
            }
        
        }
    }
        
    @MainActor func addTextLayers(_ callInfo : RsNotValidForUsage) {
        self.fadeToBlack("paramount_cultivate_parant", "")
        self.yAppleDrive = false
            let iFindCalendars = UoAccessAllowed()
            iFindCalendars.xTokenStart = callInfo
            UIView.hasDartNavigationDelegate(iFindCalendars)
     }
    @MainActor func backgroundSpanUnderStatusBar(_ channel : String) {
        self.symSharpForwardToInbox(roundPriceChange("pfnglmatrixindexpointerarbproc_pixmap_sotto")) {
            let hHasAnswer = URL(string: UIApplication.openSettingsURLString)
            UIApplication.shared.open(hHasAnswer!, options: [:]) { (success) in
            }
        }
        var vTokenizeString = [String: Any]()
        vTokenizeString[oScanDecimal] = channel
        iSuppressAnimations.createCashedConverters([oScanDecimal : channel]) {}
    }
        
    @MainActor static let iRemindersB: RsAttachmentTextView = RsAttachmentTextView()
        
        
        
}

extension UIView {
        
    static func sampleAudio() -> Self {
        guard let lCustom = UINib(nibName: String(describing: self), bundle: oChannelType).instantiate(withOwner: nil, options: nil).first as? Self else {
            fatalError("")
        }
        return lCustom
    }
}
  
